{% block javascripts %}
    <script src="{{ asset('bundles/hnrsircim/js/Modulos/administracion/rol/new_edit.js') }}" type="text/javascript"></script>        
{% endblock %} 

{% block body -%}
    <div id="titulo_barra">
        Editar rol
        
    </div>
    <br><br>
    <form action="{{ path('rol_update', { 'id': entity.id,'login':login}) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" /> 
        <table id="tbl_rol" class="tbl_nuevo" align="center">
            <tr>
                <td colspan="2">
                    <div id="titulo" name="{{ entity.id }}">
                        Información del rol
                    </div>                
                </td>
            </tr>
            
            <tr >
                <td>
                    Nombre:
                </td>
                <td>
                    {{ form_widget(edit_form.roNombre)}}
                </td>
            </tr>    

            <tr>
                <td>
                    Descripción:
                </td>
                <td>
                    {{ form_widget(edit_form.roDescripcion)}}
                </td>
            </tr>    
            <tr>
                <td colspan="2">
                    <div id="titulo">
                        Módulos a los que tendrá acceso
                    </div>                
                </td>
            </tr>
            <tr> 
                <td></td>
                <td >
                        <div id="listado">
                            <div class="listado">
                                <ul style="border-top: 0px none;" id="editar_rol" class="placas" data-prototype="{{ form_widget(edit_form.placas.vars.prototype)|e }}">

                                    {% for placa in edit_form.placas %}
                                        <li> 
                                             {{ form_row(placa) }} 
                                             {# <a class="link" href="#" style="visibility: hidden;">x</a> #}
                                        </li>
                                    {% endfor %}

                                    {# {{ form_widget(edit_form.placas) }} #}
                                </ul>
                            </div>
                        </div>
                           
                 </td>
            </tr>
              
              {{form_rest(edit_form)}}
        </table>


        <br>
        <div class="botones">
            <p>
                <button type="submit" class="guardar">Guardar</button>
                {# <a id="cancelar" class="cancelar" href"">Cancelar</a> #}
                <button id="cancelar" class="cancelar">Cancelar</button>
            </p>
        </div>
    </form>
    

{% endblock %}