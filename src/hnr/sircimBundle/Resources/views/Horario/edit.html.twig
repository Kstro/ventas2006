{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Horario edit</h1>

    <form action="{{ path('horario_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        <table>
            <tr>
                <td>
                    Día
                </td>

                <td>
                    Hora inicio
                </td>

                <td>
                    Hora fin
                </td>
            </tr>
            <tr>
                <td>
                    {{form_widget(edit_form.hoDia)}}
                </td>

                <td>
                    {{form_widget(edit_form.hoHoraInicio)}}
                </td>

                <td>
                    {{form_widget(edit_form.hoHoraFin)}}
                </td>
            </tr>
        </table>

{{ form_rest(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('horario') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('horario_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
