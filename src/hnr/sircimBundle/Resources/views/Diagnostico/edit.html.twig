
{% block body -%}
   <div id="contenido">
    <div id="transcripcion_estudios_consulta">
        <div>
            <div>
                {{ entity.idEstudioRadiologico.getIdSolicitud.getEstudioArea.getIdEstudio() }}
            </div>
            <div>
                {{ entity.idEstudioRadiologico.getIdSolicitud.getsoFecha()|date('d/m/Y') }}
                 {%if entity.idEstudioRadiologico.getIdSolicitud.getsoHora()|date('H') <= 12 %}
                - {{ entity.idEstudioRadiologico.getIdSolicitud.getsoHora()|date('H:i') }} a.m.
                {%else %}
                - {{ entity.idEstudioRadiologico.getIdSolicitud.getsoHora()|date('H:i')-12 }}{{ entity.idEstudioRadiologico.getIdSolicitud.getsoHora()|date(':i')}} p.m.
                {% endif %}
            </div>
            <div><a href="" id="cerrar_link"><img src={{ asset('bundles/hnrsircim/imagenes/cerrar.png') }} /></a></div>
      </div>
        <br><br>  

    <form action="{{ path('diagnostico_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {#{ form_widget(edit_form) }#}
       
       {# <div class="titulo2" id="radiologo">
          
                <p>Medico Radiologo:
                  <select name="empleados"> 
                <option value="">{% for empl in empl %}
                                    {{empl.Nombre}}  
                                 {% endfor %}
                </option>
                {% for empleado in idempleado %}
                <option value="{{empleado.Idempleado}}"> {{empleado.Nombre}}  </option>   
                {% endfor %}
               </select> 
            
            </p> 
            
        </div> #}
        <div class="titulo2" id="radiologo">
          
                <p>Medico Radiologo: 
                                        
                                     
                <select name="empleados"> 
                   {% for empl_actual in empl_actual %} 
                <option value="{{entity.getIdempleado}}">{{empl_actual.Nombre}}
                                    
                                 {% endfor %}
                </option>
                {% for empl in empl %}
                <option value="{{empl.Idempleado}}"> {{empl.Nombre}}  </option>   
                {% endfor %}
               </select> 
            
            </p> 
            
        </div>
        <div class="titulo2" id="resultado">
            <p>Resultado:</p>
               {{ form_row(edit_form.diResultado, {'label':' ' , 'attr':{'style':'width: 99%; resize:none; height: 275px;border:0px 
               solid;'} }) }}
          </div>    
          <div class="titulo2" id="conclusion">
              <p>Conclusi&oacute;n:</p>
               {{ form_row(edit_form.diConclusion, {'label':' ', 'attr':{'style':'width: 99%; resize:none; border:0px solid;'} }) }}
          </div>      
                
        {{ form_rest(edit_form) }}
        <div id="titulo2_botones">
           
                <button class="guardar" type="submit">Modificar</button>
        </div>
    </form>
                      
  <form action="{{ path('diagnostico_delete', { 'id': entity.id }) }}" method="post">
                        <input type="hidden" name="_method" value="DELETE" />
                          {{ form_widget(delete_form) }}
                          <div id="titulo2_botones">
                          <button type="submit">Eliminar</button>
                          </div>
                      </form>      
        
    
    

</div>
</div>
{% endblock %}
{% block script %}
    

<script type="text/javascript">


jQuery(document).ready(function() {

    $('#cancelar').click(function(event) {
        /* Act on the event */
        $('#deshabilitar').fadeOut('fast');
        $('#popup_box_exp').fadeOut('fast');
        return false;
    });

    $('#cerrar_link').click(function(event) {
        /* Act on the event */
        $('#deshabilitar').fadeOut('fast');
        $('#popup_box_exp').fadeOut('fast');
        return false;
    });






    $('.guardar').click(function(){
        
      //   var parameters = $('#historial').val();
      //   var login = $('#nombre_usr label').attr('id');
      //   $('#deshabilitar').fadeOut('fast');
      //   $('#popup_box_exp').fadeOut('fast');
      //   $('.primero').removeClass('checked');    
      //   $('.ultimo').removeClass('checked');    

      //   // $.getJSON(Routing.generate('diagnostico_exp', { exp: $parameters }), function(data) { 
      //       // $('#paciente_historial').html('<div id="cargando"><img src="'+$('#exp_label').attr('href')+'"><div>');
      //       $('#paciente_historial').html('<div id="cargando"><img src={{ asset('bundles/hnrsircim/img/loading.gif') }}><div>');

      //       $('#paciente_historial').load(Routing.generate('diagnostico_trans', { exp: parameters,login:login }));
      //       // $.each(data.regs, function(indice, reg) { 
      //       //     alert(reg.Edad);
      //       //     $('#expediente').val( reg.Edad ); 
      //       // }); 
      //   // }); 
      // return false;

    });




});

    

</script>




{% endblock %}